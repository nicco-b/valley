# PLAN_AUDIO — moved

*2026-07-13: this plan rode the E4.1b (Chord) landing into datum, the audio
organ's home. The design of record now lives at `runtime/docs/PLAN_AUDIO.md` in
the datum framework repo. This tombstone stays so old links resolve.*

See: datum `runtime/docs/PLAN_AUDIO.md`.
